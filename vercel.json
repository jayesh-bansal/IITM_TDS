{
  "version": 2,
  "builds": [{
    "src": "index.js",
    "use": "@vercel/node"  # Not @now/node [1]
  }],
  "routes": [{
    "src": "/(.*)",
    "dest": "index.js",
    "methods": ["GET", "POST", "PUT", "DELETE"]
  }]
}
